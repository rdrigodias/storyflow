# Frontend

## Scripts

- `npm run dev`: start Vite dev server.
- `npm run build`: create production build in `dist/`.

## Notes

- Keep `VITE_API_BASE` configured for API calls.
- Storyboard generation and project persistence are implemented under `src/core/`.
